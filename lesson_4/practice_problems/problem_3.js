[1, 2, 3].map(num => num * num);

// This code returns a new array containing [1, 4, 9].
// This happens because arrow functions allow for an explicit return
// on single line expressions when no braces are used.